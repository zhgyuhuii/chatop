# 对外端口
PORT=6901
# filebrowser 文件管理端口（HTTPS）
FB_PORT=8585
# 登录密码（VNC_PW）
PASSWORD=change-me
# 网页登录用户名(默认 admin)
# LOGIN_USER=admin

# === 序列号激活（可选）===
# 64 位 hex 的 HMAC 密钥，与 website 后台「支付配置 → 生成授权密钥」的 hmacKeys["1"] 同一把。
# 留空/不设 = 激活闸门关闭，登录页维持「用户名 + 密码 + 图形验证码」。
# 设了之后需重新 build（密钥在构建时烤进 /opt/chatop/license-keys.json）：
#   CHATOP_LICENSE_HMAC_KEY=<hex> VERSION=1.4.0 docker compose build
# 警告：纯离线验证意味着镜像内含对称密钥。镜像若推公开 registry，密钥即公开。
# CHATOP_LICENSE_HMAC_KEY=

# 固定本机指纹（可选）。默认指纹派生自 home 卷内的 node-id，`docker compose down -v`
# 删卷后指纹会变、需重新领序列号。设了这个就跟卷解绑。
# CHATOP_MACHINE_ID=
