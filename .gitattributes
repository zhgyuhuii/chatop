# 强制所有在 Linux 容器内执行/解析的文本用 LF 换行。
# 根因：Windows 上 git 默认 core.autocrlf=true，检出时把 LF 转成 CRLF，
# COPY 进镜像后 shebang 变 "#!/bin/bash\r"，Caddy/app-manager 静默挂掉 →
# 桌面正常但进不去登录页。此文件确保任何平台检出都保持 LF。
* text=auto eol=lf

*.sh    text eol=lf
*.py    text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
*.json  text eol=lf
*.js    text eol=lf
*.css   text eol=lf
*.html  text eol=lf
Dockerfile      text eol=lf
Caddyfile       text eol=lf
.env.example    text eol=lf

# Windows 批处理必须 CRLF：cmd.exe 对纯 LF 的 .bat 在部分结构下会异常。
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# 二进制文件不做换行转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.ico   binary
*.gif   binary
*.svg   text eol=lf
